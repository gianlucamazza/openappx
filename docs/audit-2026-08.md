# Repository audit — 2026-08

This checklist records the repository-wide audit and its verification evidence.
Durable user-facing behavior belongs in the README and guides; this file records
why the current hardening exists.

| Area | Finding | Status | Evidence |
| --- | --- | --- | --- |
| Layout security | Manifest references could point outside the layout | fixed | `tests/test_pack.py` path cases |
| Layout security | Symlink payloads could read outside the layout | fixed | `tests/test_pack.py` symlink case |
| Archive safety | Duplicate members were silently overwritten during unpack | fixed | `tests/test_unpack.py` duplicate case |
| CLI reliability | Corrupt bundle input could escape before error handling | fixed | `tests/test_cli.py` corrupt-bundle case |
| Deploy reliability | Missing dependency upload was not reported cleanly | fixed | `tests/test_deploy.py` missing-extra case |
| Output safety | Pack, bundle and sign could replace output before success | fixed | atomic writer and backend paths |
| Documentation | Device Portal examples omitted `--insecure` | fixed | README quickstart |
| Documentation | Signing, roadmap and exit-code claims were stale | fixed | README and guides |

Baseline verification is the full test suite, Ruff, Mypy, wheel and sdist smoke
tests, plus the documented example commands.
